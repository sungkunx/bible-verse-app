import React from 'react';
import ReactDOM from 'react-dom';
import BibleVerseApp from './BibleVerseApp';

ReactDOM.render(
  <React.StrictMode>
    <BibleVerseApp />
  </React.StrictMode>,
  document.getElementById('root')
);